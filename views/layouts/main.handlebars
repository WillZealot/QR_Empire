<!DOCTYPE html>
<html lang="en">
<head>
 <meta charset="UTF-8">
  <title>QR Empire</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <link rel="stylesheet"  href="./styles.css">
</head>
<body>
  {{!-- <div class="header">
    <header>
      <h1>QR Empire</h1>
    </header>
    <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout">Logout</button>
    {{else}}
    <a id="login" href="api/login">Login</a>
    <a id="dashboard" href="api/dashboard">Dashboard</a>
    {{/if}}
  </nav>
  </div> --}}
  <!-- Image and text -->
    <nav class="navbar navbar-light bg-light">
      <a class="navbar-brand">
        {{!-- <img src="/docs/4.3/assets/brand/bootstrap-solid.svg" width="30" height="30" class="d-inline-block align-top" alt=""> --}}
        QREmpire
      </a>
      <ul class="nav justify-content-end">
        {{#if logged_in}}
        <li class="nav-item" >
          <button type="button" class="btn" id="logoutBtn">Logout</button>
        </li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link active" href="/signup">Sign-Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">View Others</a>
        </li>
        {{/if}}
      </ul>
    </nav>
  <main>
      <!--iterate over each post and display any available-->
    {{{body}}}
  </main>
  {{#if logged_id}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>
